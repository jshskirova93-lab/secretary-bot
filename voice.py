
Cloud
/
Claude finished the response
ok


Used Railway integration, used 2 tools, ran a command
Used Railway integration, used 2 tools, ran a command
Готово. Исправление в работе.

Проверил всё по цепочке:

Что	Результат
Источник	jshskirova93-lab/secretary-bot, ветка main ✅
Сборка	коммит 78cf9b5 Update voice.py — SUCCESS
Запуск	«Бот запущен», polling пошёл, ошибок нет
Память сейчас	0,17 ГБ (было в среднем 0,70)
Логи чистые: планировщик поднялся, оба задания (утреннее и вечернее) на месте, Telegram отвечает. Бот живой и работает уже на новой версии.

Что это значит по деньгам
Расход по памяти упал примерно с $7 до $2 в месяц. Вместе с ботом Гузаировой (~$0,5) получается около $2,5 в месяц — это укладывается во включённые $5 тарифа Hobby с хорошим запасом.

Одна просьба — проверка
Отправьте боту голосовое сообщение. Это важно по двум причинам:

убедимся, что распознавание не сломалось после переделки;
главное — именно голосовое раньше и запускало утечку. Если после него память вернётся к 0,17–0,20 ГБ, значит починка работает по-настоящему.
Учтите: первый ответ придёт секунд на 5 позже обычного — модель теперь подгружается заново. Это нормально, так и задумано. Дальнейшие голосовые в течение сессии будут такими же.

Напишите, как бот справился с голосовым. Завтра вечером сниму суточный замер и покажу честную картину — тогда и выберем тариф. До конца пробного периода 6 дней, время есть.


Hosting i ekonomiya
Document · MD 
Будд справился хорошо. В принципе, такая же задержка была и раньше. Разницы не заметил.


Ran 2 commands, used Railway integration
Ran 2 commands, used Railway integration
Проверил замеры — и должен вас поправить, пока не поздно.


Created 2 files, ran a command, used a tool
Created 2 files, ran a command, used a tool
Хорошо, что задержки нет. Но с памятью я поспешил с выводами — и должен это признать.

Что показали замеры
Я снял метрики до и после вашего голосового:

Момент	Память
После перезапуска, до голосового	0,17 ГБ ✅
Сразу после голосового	0,63 ГБ
Через 6 минут после	0,64 ГБ ❌
Память не вернулась. И через шесть минут держится на том же уровне.

Почему моё исправление не сработало
Я удалял объект модели и вызывал сборщик мусора. Внутри Python модель действительно исчезает — но операционная система не забирает освобождённую память обратно у процесса. Она остаётся числиться за ботом, а Railway берёт деньги именно за занятую память, а не за «полезно используемую».

Проще говоря: я вымыл посуду, но не вынес ведро. Место в кухне не освободилось.

Это моя ошибка в рассуждении — тест с заглушкой её не поймал, потому что заглушка проверяла освобождение объекта, а не реальную память процесса. Проверять надо было на живой системе, что мы сейчас и сделали.

Исправление №2 — уже готово и проверено
Теперь распознавание вынесено в отдельный процесс, который запускается на одно голосовое и сразу завершается. Когда процесс завершается, система забирает всю его память целиком — тут уже вариантов нет, это гарантия уровня операционной системы.

Проверил тестом с имитацией модели на 300 МБ:

текст распознаётся правильно;
память основного процесса: рост 0 МБ (раньше в этом месте было +300);
временные файлы не копятся;
при сбое распознавания бот не падает, пишет причину в лог и сообщает об ошибке;
есть предохранитель: если распознавание зависнет, процесс убивается через 5 минут.
Что нужно сделать
Файлов теперь два. Первый заменяем, второй создаём новый.

1. Заменить voice.py — как в прошлый раз:

https://github.com/jshskirova93-lab/secretary-bot/edit/main/voice.py
Ctrl+A → вставить новый текст → Commit changes.

2. Создать voice_worker.py — новый файл:

https://github.com/jshskirova93-lab/secretary-bot/new/main
В поле имени файла введите: voice_worker.py
В большое поле вставьте текст второго файла
Commit changes
Теперь автодеплой настроен, так что Railway подхватит изменения сам — вмешиваться не придётся.

После сборки снова отправьте боту голосовое, и я сразу сниму замер. Если память вернётся к 0,17 ГБ — значит, добили по-настоящему.


Voice
PY 

Voice worker
PY 
В последнем сообщении два файла, который из них правильный

Оба правильные — но они разные и нужны оба. Это не два варианта одного файла, а два разных файла.

Как отличить. Посмотрите на самую первую строчку текста внутри:

Файл	Первая строка внутри	Что с ним делать
voice.py	«Распознавание голосовых сообщений.»	Заменить существующий
voice_worker.py	«Рабочий процесс распознавания речи.»	Создать новый
Имя файла также видно в самой карточке в чате — под ней подписано voice.py и voice_worker.py.

Зачем два. Раньше бот всё делал сам в одиночку и оставался «раздутым» после распознавания. Теперь работа разделена:

voice.py — остаётся в боте, он поручает работу и принимает результат;
voice_worker.py — отдельный работник, которого зовут на одно голосовое. Он делает работу и уходит, забирая с собой всю занятую память.
Без второго файла первый работать не будет — ему некого будет звать.

Начните с замены voice.py:

https://github.com/jshskirova93-lab/secretary-bot/edit/main/voice.py
Как закоммитите — напишите, и дам ссылку на создание второго. Пойдём по одному, чтобы не перепутать.






Claude is AI and can make mistakes. Please double-check responses. Give us feedback
Voice worker · PY
"""
Рабочий процесс распознавания речи. Запускается отдельно, на одно голосовое.
 
Зачем отдельный процесс: библиотека faster-whisper выделяет под модель сотни
мегабайт, и после её выгрузки внутри Python операционная система эту память
обратно НЕ забирает — она остаётся числиться за процессом бота, и хостинг за
неё берёт деньги. А когда завершается отдельный процесс, система забирает всю
его память целиком и гарантированно. Поэтому распознаём здесь и сразу выходим.
 
Принимает: путь к .ogg файлу первым аргументом.
Возвращает: распознанный текст в стандартный вывод.
"""
import os
import sys
 
 
def main() -> int:
    if len(sys.argv) < 2:
        sys.stderr.write("Не передан путь к аудиофайлу\n")
        return 2
 
    audio_path = sys.argv[1]
 
    # Размер модели можно менять переменной окружения, не трогая код:
    #   tiny  — ~75 МБ,  быстрая, качество похуже
    #   base  — ~145 МБ, разумный компромисс (по умолчанию)
    #   small — ~500 МБ, точнее всех, но медленнее
    model_size = os.getenv("WHISPER_MODEL", "base")
 
    # Импорт внутри функции: тяжёлая библиотека грузится только здесь,
    # в основном процессе бота её вообще нет.
    from faster_whisper import WhisperModel
 
    # compute_type="int8" — работает на CPU без видеокарты, приемлемая скорость
    model = WhisperModel(model_size, device="cpu", compute_type="int8")
 
    # transcribe() возвращает генератор — текст реально считывается только в join()
    segments, _info = model.transcribe(audio_path, language="ru")
    text = " ".join(segment.text.strip() for segment in segments).strip()
 
    sys.stdout.write(text)
    sys.stdout.flush()
    return 0
 
 
if __name__ == "__main__":
    sys.exit(main())
 

Press and hold to recordCtrl+D
